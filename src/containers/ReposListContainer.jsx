import React, { Component, Fragment } from 'react';
import PropTypes from 'prop-types';
import ReposList from '../components/ReposList';

export default class ReposListContainer extends Component {
  render() {
    return (
      <ReposList />
    );
  }
}
